# INZZO Landing Page

![INZZO Logo](static/img/logo.PNG)

## 🎯 Описание проекта

Landing Page для бренда уличной одежды **INZZO**. Сайт-заглушка в стиле Dark Streetwear с Asian aesthetic, созданный для сбора базы клиентов (вайтлист) перед запуском коллекции.

## 🎨 Особенности

- **Dark Streetwear эстетика** с минимализмом
- **Адаптивный дизайн** для всех устройств
- **Glitch эффекты** на заголовке
- **Автоматические уведомления** в Telegram при новых заявках
- **Валидация форм** на фронтенде и бэкенде

## 🏗️ Технологический стек

### Frontend
- HTML5 + CSS3 (Flexbox/Grid)
- Vanilla JavaScript (Fetch API)
- Google Fonts (Montserrat)

### Backend
- Python 3.8+
- Flask (веб-фреймворк)
- Telegram Bot API
- python-dotenv

## 📦 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <your-repo-url>
cd inzzo
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Скопируйте токен бота
3. Узнайте свой Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```
5. Заполните `.env` вашими данными:
```
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

### 4. Запуск сервера
```bash
python app.py
```

Сайт будет доступен по адресу: `http://localhost:5000`

## 📁 Структура проекта

```
inzzo/
├── app.py                 # Flask бэкенд
├── index.html            # Главная страница
├── requirements.txt      # Python зависимости
├── .env                  # Конфигурация (не в Git!)
├── .env.example         # Пример конфигурации
├── static/
│   ├── styles.css       # Стили
│   ├── script.js        # JavaScript логика
│   └── img/
│       ├── logo.PNG
│       ├── red_hoodie.JPEG
│       └── black_hoodie.JPEG
└── README.md
```

## 🚀 Деплой в продакшн

### Vercel (Рекомендуется) ⭐
```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Войдите в аккаунт
vercel login

# 3. Деплой проекта
vercel

# 4. Добавьте переменные окружения
vercel env add TELEGRAM_BOT_TOKEN
vercel env add TELEGRAM_CHAT_ID

# 5. Продакшн деплой
vercel --prod
```

**Или через веб-интерфейс Vercel:**
1. Зайдите на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Vercel автоматически определит Flask проект
4. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `TELEGRAM_CHAT_ID` - ID чата для уведомлений
5. Нажмите Deploy

### Heroku
```bash
# Установите Heroku CLI
# Создайте Procfile
echo "web: gunicorn app:app" > Procfile

# Добавьте gunicorn в requirements.txt
echo "gunicorn==21.2.0" >> requirements.txt

# Деплой
heroku create inzzo-landing
git push heroku main
heroku config:set TELEGRAM_BOT_TOKEN=your_token
heroku config:set TELEGRAM_CHAT_ID=your_chat_id
```

### Render / Railway
1. Подключите GitHub репозиторий
2. Укажите команду запуска: `gunicorn app:app`
3. Добавьте переменные окружения в панели управления

## 🎨 Кастомизация

### Цвета
Отредактируйте CSS переменные в `static/styles.css`:
```css
:root {
    --black: #000000;
    --accent-red: #ff0033;
    /* ... */
}
```

### Telegram сообщения
Измените формат в функции `send_telegram_notification()` в `app.py`

### Контент
Отредактируйте текст в `index.html`

## 📝 API Endpoints

### POST /api/submit-lead
Принимает заявку от клиента

**Request:**
```json
{
  "name": "Иван",
  "email": "ivan@example.com",
  "timestamp": "2025-12-16T10:30:00Z"
}
```

**Response (Success):**
```json
{
  "success": true,
  "message": "Спасибо! Ты в списке.",
  "telegram_sent": true
}
```

### GET /health
Проверка статуса сервера

## 🔒 Безопасность

- ✅ Валидация на фронтенде и бэкенде
- ✅ Защита от SQL-инъекций (при добавлении БД)
- ✅ CORS настроен для безопасности
- ✅ `.env` файл в `.gitignore`

## 📊 Метрики и аналитика

Для отслеживания конверсий можно добавить:
- Google Analytics
- Yandex.Metrica
- Facebook Pixel

## 🤝 Разработка

Создано для бренда **INZZO**  
© 2025 Все права защищены

---

**Нужна помощь?** Создайте Issue в репозитории

# 🐛 TROUBLESHOOTING: Telegram уведомления не приходят

## ✅ CHECKLIST для проверки

### 1️⃣ Проверьте переменные окружения в Vercel

**ВАЖНО!** В Vercel должны быть установлены переменные окружения:

```
TELEGRAM_BOT_TOKEN=8510673777:AAFZIzPXQovocBBCyKEQQSUTLdODwC3nlSo
TELEGRAM_CHAT_ID=7099490320
```

**Как проверить:**
1. Откройте https://vercel.com/dashboard
2. Выберите проект `inzzo`
3. Settings → Environment Variables
4. Убедитесь что есть обе переменные

**Если переменных нет - добавьте их:**
- Нажмите "Add New"
- Name: `TELEGRAM_BOT_TOKEN`
- Value: `8510673777:AAFZIzPXQovocBBCyKEQQSUTLdODwC3nlSo`
- Environments: Production, Preview, Development
- Save

Повторите для `TELEGRAM_CHAT_ID`

**После добавления переменных нужно REDEPLOY проект!**

---

### 2️⃣ Проверьте запустили ли вы бота

1. Откройте Telegram
2. Найдите вашего бота в поиске
3. Нажмите **START** или отправьте `/start`

❌ **Без этого бот не сможет отправлять сообщения!**

---

### 3️⃣ Проверьте правильность Chat ID

**Ваш Chat ID:** `7099490320`

Чтобы убедиться что это правильный ID:
1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. Проверьте, совпадает ли ID

---

### 4️⃣ Проверьте работу endpoint

Откройте в браузере:
```
https://ваш-проект.vercel.app/health
```

**Правильный ответ:**
```json
{
  "status": "ok",
  "telegram_configured": true
}
```

**Если `telegram_configured: false`** - значит переменные окружения не установлены в Vercel!

---

### 5️⃣ Проверьте логи Vercel

1. Откройте https://vercel.com/dashboard
2. Выберите проект `inzzo`
3. Deployments → выберите последний деплой
4. Runtime Logs (или Function Logs)
5. Отправьте тестовую заявку на сайте
6. Посмотрите логи - там будет ошибка если что-то не так

**Возможные ошибки:**
- `❌ Telegram not configured` - нет переменных окружения
- `Telegram API error: 401` - неверный токен бота
- `Telegram API error: 400` - неверный Chat ID или бот не запущен

---

### 6️⃣ Тест Telegram API вручную

Проверьте работу вашего бота напрямую:

```bash
curl -X POST "https://api.telegram.org/bot8510673777:AAFZIzPXQovocBBCyKEQQSUTLdODwC3nlSo/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": "7099490320",
    "text": "Test message from INZZO"
  }'
```

**Если получите ошибку:**
- `"error_code": 401` - неверный токен
- `"error_code": 400, "description": "Bad Request: chat not found"` - бот не запущен
- `"error_code": 403` - бот заблокирован

---

## 🔧 РЕШЕНИЯ частых проблем

### Проблема 1: telegram_configured = false

**Решение:**
1. Добавьте переменные окружения в Vercel (см. шаг 1)
2. После добавления сделайте Redeploy:
   - Deployments → выберите последний деплой → три точки → Redeploy

### Проблема 2: Ошибка 400 - chat not found

**Решение:**
1. Найдите бота в Telegram
2. Нажмите START
3. Попробуйте снова

### Проблема 3: Ошибка 401 - Unauthorized

**Решение:**
1. Проверьте токен бота в @BotFather
2. Обновите переменную TELEGRAM_BOT_TOKEN в Vercel
3. Redeploy

### Проблема 4: Форма отправляется, но нет уведомления

**Решение:**
1. Проверьте логи в Vercel
2. Убедитесь что переменные установлены
3. Redeploy после установки переменных

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Проверка endpoint
```bash
curl https://ваш-проект.vercel.app/health
```

### Тест 2: Прямая отправка через Telegram API
```bash
curl -X POST "https://api.telegram.org/bot8510673777:AAFZIzPXQovocBBCyKEQQSUTLdODwC3nlSo/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": "7099490320", "text": "Test"}'
```

### Тест 3: Отправка заявки через форму
1. Откройте сайт
2. Заполните форму
3. Проверьте логи в Vercel
4. Проверьте Telegram

---

## 📋 ПОШАГОВЫЙ PLAN исправления

1. ✅ Запустите бота в Telegram (START)
2. ✅ Добавьте переменные окружения в Vercel
3. ✅ Redeploy проект
4. ✅ Проверьте /health endpoint
5. ✅ Отправьте тестовую заявку
6. ✅ Проверьте логи если не работает

---

## 🚀 БЫСТРОЕ РЕШЕНИЕ (90% случаев)

```bash
# 1. Проверьте что vercel.json не пустой
cat vercel.json

# 2. Сделайте push обновленного vercel.json
git add vercel.json
git commit -m "Fix vercel.json"
git push

# 3. В Vercel добавьте переменные окружения (если не добавили)
# 4. Redeploy проект в Vercel
```

---

## ❓ Нужна помощь?

Отправьте мне:
1. URL вашего проекта на Vercel
2. Скриншот ответа /health endpoint
3. Скриншот логов из Vercel

# 🚀 Деплой INZZO на Vercel

## Быстрый старт

### Способ 1: Через Vercel CLI (Рекомендуется)

1. **Установите Vercel CLI**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт Vercel**
   ```bash
   vercel login
   ```
   Следуйте инструкциям для авторизации через браузер.

3. **Запустите деплой**
   ```bash
   vercel
   ```
   При первом запуске Vercel задаст несколько вопросов:
   - Set up and deploy? → **Yes**
   - Which scope? → Выберите ваш аккаунт
   - Link to existing project? → **No**
   - What's your project's name? → **inzzo** (или другое имя)
   - In which directory is your code located? → **./** (просто нажмите Enter)

4. **Добавьте переменные окружения**
   ```bash
   vercel env add TELEGRAM_BOT_TOKEN
   ```
   Вставьте токен вашего Telegram бота и выберите окружения:
   - Production: **Yes**
   - Preview: **Yes**
   - Development: **Yes**

   ```bash
   vercel env add TELEGRAM_CHAT_ID
   ```
   Вставьте ваш Chat ID и выберите те же окружения.

5. **Деплой в продакшн**
   ```bash
   vercel --prod
   ```

   Готово! Ваш сайт будет доступен по адресу типа `https://inzzo.vercel.app`

---

### Способ 2: Через веб-интерфейс Vercel

1. **Создайте аккаунт на Vercel**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите "Sign Up"
   - Войдите через GitHub

2. **Загрузите проект на GitHub**
   ```bash
   # Инициализируйте git (если еще не сделали)
   git init
   git add .
   git commit -m "Initial commit for Vercel deployment"
   
   # Создайте репозиторий на GitHub и загрузите код
   git remote add origin https://github.com/ваш-username/inzzo.git
   git branch -M main
   git push -u origin main
   ```

3. **Импортируйте проект в Vercel**
   - В панели Vercel нажмите "Add New Project"
   - Выберите "Import Git Repository"
   - Найдите свой репозиторий `inzzo`
   - Нажмите "Import"

4. **Настройте проект**
   - **Framework Preset:** Vercel автоматически определит "Other"
   - **Root Directory:** `.` (оставьте по умолчанию)
   - **Build Command:** оставьте пустым
   - **Output Directory:** оставьте пустым

5. **Добавьте переменные окружения**
   - Нажмите "Environment Variables"
   - Добавьте:
     - Name: `TELEGRAM_BOT_TOKEN` → Value: ваш токен бота
     - Name: `TELEGRAM_CHAT_ID` → Value: ваш chat ID
   - Нажмите "Deploy"

6. **Дождитесь завершения деплоя**
   - Vercel покажет процесс сборки
   - После завершения появится кнопка "Visit"
   - Ваш сайт готов!

---

## 🔧 Настройка Telegram бота

Если вы еще не настроили Telegram бота:

1. **Создайте бота**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Придумайте имя: `INZZO Leads Bot`
   - Придумайте username: `inzzo_leads_bot`
   - Скопируйте токен (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Получите Chat ID**
   - Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
   - Отправьте любое сообщение
   - Скопируйте ваш ID (например: `123456789`)

3. **Запустите бота**
   - Найдите вашего бота в Telegram
   - Нажмите "Start"

---

## 📝 Проверка работы

После деплоя проверьте:

1. **Откройте сайт**
   ```
   https://ваш-проект.vercel.app
   ```

2. **Проверьте health endpoint**
   ```
   https://ваш-проект.vercel.app/health
   ```
   Должен вернуть:
   ```json
   {
     "status": "ok",
     "telegram_configured": true
   }
   ```

3. **Отправьте тестовую заявку**
   - Заполните форму на сайте
   - Проверьте, что уведомление пришло в Telegram

---

## 🔄 Обновление проекта

При каждом push в GitHub, Vercel автоматически задеплоит новую версию:

```bash
git add .
git commit -m "Update"
git push
```

Или через CLI:
```bash
vercel --prod
```

---

## 🌐 Настройка домена

### Подключить свой домен:

1. В панели Vercel откройте ваш проект
2. Перейдите в "Settings" → "Domains"
3. Нажмите "Add"
4. Введите ваш домен (например: `inzzo.ru`)
5. Следуйте инструкциям для настройки DNS

**Пример настройки DNS:**
- Тип: `A` → IP: `76.76.21.21`
- Тип: `CNAME` → Значение: `cname.vercel-dns.com`

---

## 🐛 Решение проблем

### Проблема: "Module not found"
**Решение:** Проверьте `requirements.txt`, все зависимости должны быть указаны.

### Проблема: "Telegram not configured"
**Решение:** 
1. Проверьте переменные окружения в Vercel
2. Убедитесь, что имена переменных точно совпадают:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`

### Проблема: 500 Internal Server Error
**Решение:**
1. Откройте логи в Vercel: проект → "Deployments" → выберите деплой → "View Function Logs"
2. Проверьте ошибки в логах

### Проблема: Static files не загружаются
**Решение:** Убедитесь, что `vercel.json` настроен правильно и статические файлы находятся в папке `static/`.

---

## 📊 Мониторинг

### Просмотр логов:
1. Откройте проект в Vercel
2. Перейдите в "Deployments"
3. Выберите нужный деплой
4. Нажмите "View Function Logs"

### Просмотр аналитики:
1. В проекте перейдите в "Analytics"
2. Здесь вы увидите:
   - Количество посетителей
   - География
   - Производительность

---

## ✅ Checklist перед деплоем

- [ ] Файл `vercel.json` создан
- [ ] Файл `.vercelignore` создан
- [ ] В `requirements.txt` все зависимости
- [ ] Telegram бот создан и настроен
- [ ] Переменные окружения готовы
- [ ] Код протестирован локально
- [ ] Git репозиторий создан (для веб-интерфейса)

---

## 🎉 Готово!

После успешного деплоя ваш сайт INZZO будет доступен 24/7 с автоматическими обновлениями при каждом коммите!

**Полезные ссылки:**
- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel Documentation](https://vercel.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
